import React from 'react';
import { BrowserRouter as Router, Route, Link, Redirect } from 'react-router-dom';

const default_comp = (props_sent) => {

    return <Redirect to="/" />;

    
}


export default default_comp;
